import React from 'react';

const TodoItem = ({id, todo, isCompleted, userId}) => {
    return (
        <li>{id, todo, isCompleted, userId}</li>
    );
};

"id": 2,
        "todo": "영화보기",
        "isCompleted": true,
        "userId": 2,
export default TodoItem;